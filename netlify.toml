[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Disable secret scanning - these are public Supabase keys protected by RLS
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
